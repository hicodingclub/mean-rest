// This is a modified file from original generated "file.component.ts", and only leave the FileRefSelectDirective
// This directive is used by mfile/mfile.component.ts
import {
  ViewContainerRef,
  Directive
} from '@angular/core';
@Directive({
  selector: '[file-ref-select]',
})
export class FileRefSelectDirective {
  constructor(public viewContainerRef: ViewContainerRef) {}
}
